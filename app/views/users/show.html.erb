<%= render "shared/navbar" %>

<style>
  .user-details {
    max-width: 600px;
    margin: 20px auto;
    padding: 20px;
    background-color: #f9f9f9;
    border: 1px solid #ccc;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }

  .user-details p {
    margin-bottom: 10px;
  }

  .btn {
    padding: 10px 20px;
    background-color: DeepSkyBlue;
    border: none;
    color: white;
    text-decoration: none;
    display: inline-block;
    margin-right: 10px;
    margin-bottom: 10px;
    cursor: pointer;
    border-radius: 5px;
    transition: background-color 0.3s ease, box-shadow 0.3s ease;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }

  .btn:hover {
    background-color: #1E90FF;
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
  }
</style>

<h1>User Details</h1>

<div class="user-details">
  <p><strong>Nome:</strong> <%= @user.name %></p>
  <p><strong>Telefone:</strong> <%= @user.phone_number %></p>
  <p><strong>Produtos:</strong> <%= @user.bio %></p>

  <%= link_to 'Editar', edit_user_path(@user), class: 'btn btn-primary' %>
  <%= link_to 'Voltar', users_path, class: 'btn btn-secondary' %>
  <%= button_to "Deletar Usuário", @user, method: :delete, class: 'btn' %>
</div>
